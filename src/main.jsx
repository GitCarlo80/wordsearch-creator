import React from 'react'
import ReactDOM from 'react-dom/client'
import WordSearchCreator from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WordSearchCreator />
  </React.StrictMode>,
)
